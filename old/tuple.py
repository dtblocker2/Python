#list ka judwaa bhai. these are list with unchangeable data
#to make tuple use () instead of []
tup = (2, 1, 4, 4, 3)
'''all operrations like below, indexing and also slicing (except those which can change data in tuple) are allowed in tuple '''
print(tup.count(4))
print(len(tup))